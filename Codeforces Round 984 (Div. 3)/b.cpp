#include <bits/stdc++.h>

using namsespace std;

int main() {
    int t;
    cin >> t;
    while (t > 0) {
        int s, b;
        cin >> s;
        cin >> b;

        for 


        t--;
    }
}